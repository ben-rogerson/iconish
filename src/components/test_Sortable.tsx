// import { render } from '@testing-library/react';
// import useAppStore from 'path/to/useAppStore';
// import Sortable from './Sortable';

// jest.mock('path/to/useAppStore');

// describe('Sortable', () => {
//   it('should render with the correct hash', () => {
//     const mockHash = 'abc123';
//     useAppStore.mockReturnValueOnce(mockHash);

//     const { getByTestId } = render(<Sortable />);
//     const hashElement = getByTestId('hash');

//     expect(hashElement.textContent).toBe(mockHash);
//   });
// });
